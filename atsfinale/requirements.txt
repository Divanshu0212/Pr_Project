# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0

# NLP — spaCy is excluded (exceeds Vercel's 250MB bundle limit).
# The app detects its absence and falls back to NLTK-only analysis.
nltk==3.8.1
scikit-learn==1.3.2
numpy==1.24.4

# AI / LLM
groq==0.4.1

# HTTP / async
aiohttp==3.9.1
requests==2.31.0
beautifulsoup4==4.12.2

# Data validation
pydantic==2.5.0

# Env vars (local dev only; Vercel injects env vars natively)
python-dotenv==1.0.0
